# Consult the documentation on the mean() function
help(mean)
#help는 mean의 전체 레퍼런스를 별도의 창으로 보여줌.

# Inspect the arguments of the mean() function
#mean에 필요한 파라미터들을 바로 보여줌. good. 자주 이용하기.

args(mean)


Ex.
> help(sample)
> args(sample)
function (x, size, replace = FALSE, prob = NULL)
NULL
